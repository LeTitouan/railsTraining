<h3>Users :</h3>

<% @users.each do |user| %>
  <h4><%= user.username %></h4>

  <a href="<%= user_path(user.id) %>" class="btn btn-primary">details</a>
<%end%>

<br>
<br>
<br>

<a href="<%= new_post_path %>" class="btn btn-success">New post</a>
